apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance-service
  namespace: inventory
  labels:
    app: finance-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: finance-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: finance-service
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: finance-service
        image: ghcr.io/inventory-management-system/finance-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8092
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: JAVA_OPTS
          value: "-Xms256m -Xmx512m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+UseStringDeduplication"
        - name: NACOS_SERVER_ADDR
          value: "nacos:8848"
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: database-config
              key: host
              optional: true
        - name: DB_PORT
          valueFrom:
            secretKeyRef:
              name: database-config
              key: port
              optional: true
        - name: DB_NAME
          value: "inventory_finance_db"
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: database-credentials
              key: username
              optional: true
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-credentials
              key: password
              optional: true
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
            ephemeral-storage: "128Mi"
          limits:
            memory: "512Mi"
            cpu: "500m"
            ephemeral-storage: "256Mi"
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
              - ALL
        volumeMounts:
        - name: tmp-volume
          mountPath: /tmp
        - name: logs-volume
          mountPath: /var/log/app
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8092
            scheme: HTTP
          initialDelaySeconds: 40
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8092
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: tmp-volume
        emptyDir: {}
      - name: logs-volume
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: finance-service
  namespace: inventory
  labels:
    app: finance-service
spec:
  type: ClusterIP
  selector:
    app: finance-service
  ports:
    - protocol: TCP
      port: 8092
      targetPort: 8092
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: finance-service-config
  namespace: inventory
data:
  application.yml: |
    server:
      port: 8092
    spring:
      application:
        name: finance-service
      profiles:
        active: prod
      cloud:
        nacos:
          discovery:
            server-addr: ${NACOS_SERVER_ADDR:nacos:8848}
          config:
            server-addr: ${NACOS_SERVER_ADDR:nacos:8848}
    management:
      endpoints:
        web:
          exposure:
            include: health,info,metrics,prometheus
      health:
        redis:
          enabled: true
        prometheus:
          enabled: true
      endpoint:
        health:
          show-details: when-authorized
          probes:
            enabled: true
